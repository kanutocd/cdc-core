# frozen_string_literal: true

RSpec.describe Cdc::Core do
  it "has a version number" do
    expect(Cdc::Core::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
